# 项目文档总览（合并版）

目的：将现有散落的工作文档与测试文档合并为 1-2 个必要文档，便于查阅与维护。

保留的主要文档（作为唯一必要入口）：

1. QUICK_START.md —— 快速开始 / 使用与配置要点（已保留完整文件）。
2. IMPLEMENTATION_SUMMARY.md —— 实现与变更总结（已保留完整文件）。

本文件为整合索引，包含：核心摘要、重要链接与快速检查清单，供日常使用者和维护者参考。

核心摘要：

- 用户流程：锁预算（快捷按钮）→ 定配置（RAM/SSD）→ 找机型（搜索下拉）。
- 主要模块：价格筛选（双滑块 + 快捷档位 + 容错）、全局搜索（1 字符触发 + 模糊匹配）、已选条件标签（可删除）。
- 体验要点：PC 端 300ms debounce 实时同步；移动端保留“应用筛选”流程；渐进式渲染与骨架屏用于列表加载优化。

必要检查清单（部署前）：
- npm run build 成功。
- 搜索下拉响应 < 100ms。
- 价格筛选容错与快捷按钮功能正常。
- 产品列表在常见设备上渲染流畅（FPS > 50）。

快速链接（仓库根目录）：
- QUICK_START.md
- IMPLEMENTATION_SUMMARY.md
- TESTING_GUIDE.md （如需详细测试步骤，可参阅）

若要查看被合并/归档的旧文档及其摘要，请查阅 ARCHIVE_INDEX.md。